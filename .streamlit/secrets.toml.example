# Solo en el servidor: nunca compartir ni versionar secrets.toml.
SUPABASE_URL = ""
SUPABASE_KEY = "" # Secret key / service_role; nunca anon ni publishable.
BASEP_ACCESS_PASSWORD = "" # Contraseña privada del piloto (mínimo 16 caracteres).
# Sin estas opciones se mantiene el JSON de demostración.
